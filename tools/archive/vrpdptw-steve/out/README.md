This directory is where an sequence of images are written for each iteration
of the tabu search. They can be view as an animated sequence using the
ImageMagick animate command:

```
animate out/*.png
```
